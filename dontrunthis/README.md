Installation Guide:\
#update\
`sudo apt update`\
#install some random library in which i dont really know how it works but you definitly need it\
`sudo apt install libasound2-dev`\
#git clone teh silly stuff\
`git clone https://github.com/SteamPunk424/SillyStuff`\
#change directory into the silly folder\
`cd SillyStuff/dontrunthis/`\
#create virtual env with python3\
`python3 -m venv venv`\
#activate virtual env\
`source venv/bin/activate`\
#install requirements\
`pip install -r requirements.txt`
